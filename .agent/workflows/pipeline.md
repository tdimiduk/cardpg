---
description: Switch context to Python data pipeline tools
---

# Context: Data Pipeline

- **Directory**: `tools`
- **Language**: Python
- **Key Script**: `run_pipeline.py`
- **Execution**: MUST run `uv run run_pipeline.py` from inside the `tools` directory.
- **Purpose**: Generates `generated_cards.json` from YAML/Haskell sources.
