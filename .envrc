#!/usr/bin/env bash

export DIRENV_WARN_TIMEOUT=20s

# Use haskell.nix devShell with correct GHC 9.12
use flake

# Add project scripts to PATH (must be after use flake)
PATH_add scripts

dotenv .env.local
