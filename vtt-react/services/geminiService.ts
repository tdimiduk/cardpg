import { Token } from "../types";

// AI functionality has been disabled for playtesting focus.

export const generateFlavorText = async (prompt: string, contextTokens: Token[]): Promise<string> => {
  return "AI features are currently disabled.";
};

export const generateNpcAction = async (monsterName: string, situation: string): Promise<string> => {
  return "AI features are currently disabled.";
};